import { Router } from "express";

const folderrouter = Router();

folderrouter.get("folders",()=>{})
folderrouter.post("folders",()=>{})
folderrouter.delete("folders",()=>{})
folderrouter.put("folders",()=>{})

export default folderrouter;